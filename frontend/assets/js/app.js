const API='../backend/api.php';
async function api(action, options={}){const opts={credentials:'same-origin',headers:{'Content-Type':'application/json',...(options.headers||{})},...options};if(opts.body&&typeof opts.body!=='string')opts.body=JSON.stringify(opts.body);const res=await fetch(`${API}?action=${encodeURIComponent(action)}`,opts);let json;try{json=await res.json()}catch{throw new Error('The server returned an invalid response.')}if(!res.ok||!json.success)throw new Error(json.message||'Request failed.');return json}
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const fmtDate=d=>d?new Date(d+'T00:00:00').toLocaleDateString(undefined,{year:'numeric',month:'short',day:'numeric'}):'';
const fmtTime=t=>t?new Date('1970-01-01T'+t).toLocaleTimeString(undefined,{hour:'2-digit',minute:'2-digit'}):'';
function toast(message,type='success'){const el=document.createElement('div');el.className=`alert alert-${type} shadow rounded-3 mb-2`;el.textContent=message;document.querySelector('.toast-wrap').append(el);setTimeout(()=>el.remove(),3500)}
function statusBadge(s){const cls={Upcoming:'primary',Ongoing:'success',Completed:'secondary',Cancelled:'danger'}[s]||'dark';return `<span class="badge bg-${cls} rounded-pill">${esc(s)}</span>`}
function layout(title,active=''){document.title=title+' - NSBM EventHub';document.body.innerHTML=`<nav class="navbar navbar-expand-lg navbar-dark navbar-nsbm sticky-top"><div class="container page-shell"><a class="navbar-brand d-flex align-items-center gap-2" href="index.html"><span class="navbar-brand-badge">NSBM</span><b>EventHub</b></a><button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#nav"><span class="navbar-toggler-icon"></span></button><div id="nav" class="collapse navbar-collapse"><ul class="navbar-nav me-auto" id="navLinks"></ul><div id="userNav"></div></div></div></nav><main class="container page-shell py-4"><div class="toast-wrap"></div><div id="app"></div></main><footer class="footer mt-5 py-4"><div class="container page-shell d-flex flex-column flex-md-row justify-content-between gap-2"><span>NSBM EventHub © 2026 NSBM Green University</span><span>University Event Planning & Scheduling System</span></div></footer>`;return document.getElementById('app')}
async function setupNav(){try{const r=await api('session');const links=document.getElementById('navLinks'),u=document.getElementById('userNav');if(!links)return;const prefix=(location.pathname.includes('/student/')||location.pathname.includes('/admin/'))?'../':'';if(!r.data.logged_in){links.innerHTML=`<li class="nav-item"><a class="nav-link" href="${prefix}student/events_browse.html">Browse Events</a></li><li class="nav-item"><a class="nav-link" href="${prefix}login.html">Login</a></li>`;u.innerHTML=`<a class="btn btn-light btn-sm" href="${prefix}register.html">Register</a>`;return}const role=r.data.user.role;if(role==='admin'){links.innerHTML=`<li class="nav-item"><a class="nav-link" href="${prefix}admin/dashboard.html">Dashboard</a></li><li class="nav-item"><a class="nav-link" href="${prefix}admin/events_manage.html">Events</a></li><li class="nav-item"><a class="nav-link" href="${prefix}admin/categories_manage.html">Categories</a></li><li class="nav-item"><a class="nav-link" href="${prefix}admin/announcements_manage.html">Announcements</a></li><li class="nav-item"><a class="nav-link" href="${prefix}admin/registrations_view.html">Participants</a></li>`}else{links.innerHTML=`<li class="nav-item"><a class="nav-link" href="${prefix}student/dashboard.html">Dashboard</a></li><li class="nav-item"><a class="nav-link" href="${prefix}student/events_browse.html">Browse Events</a></li><li class="nav-item"><a class="nav-link" href="${prefix}student/my_schedule.html">My Schedule</a></li><li class="nav-item"><a class="nav-link" href="${prefix}student/announcements_view.html">Announcements</a></li>`}u.innerHTML=`<span class="text-white-50 me-2 d-none d-lg-inline">${esc(r.data.user.full_name)}</span><button class="btn btn-outline-light btn-sm" id="logoutBtn">Logout</button>`;document.getElementById('logoutBtn').onclick=async()=>{await api('logout',{method:'POST',body:{}});location.href='index.html'}}catch{}}
async function requireRole(role){const r=await api('session');if(!r.data.logged_in){location.href='../login.html';return null}if(r.data.user.role!==role){location.href=role==='admin'?'../student/dashboard.html':'../admin/dashboard.html';return null}return r.data.user}
function eventCard(e){const pct=Math.min(100,Math.round((e.registered_count/e.capacity)*100));const action=e.is_registered?`<a class="btn btn-outline-success rounded-pill" href="event_details.html?id=${e.id}">Registered · Details</a>`:e.is_full?`<a class="btn btn-outline-danger rounded-pill" href="event_details.html?id=${e.id}">Fully Booked</a>`:`<a class="btn btn-nsbm rounded-pill" href="event_details.html?id=${e.id}">Reserve Seat</a>`;return `<div class="col-md-6 col-lg-4 event-item" data-search="${esc((e.title+' '+e.venue+' '+e.description).toLowerCase())}" data-category="${e.category_id}"><div class="glass-card p-4 h-100 d-flex flex-column event-card"><div class="d-flex justify-content-between gap-2 mb-3"><span class="badge bg-success bg-opacity-10 text-success border border-success">${esc(e.category_name)}</span>${statusBadge(e.status)}</div><h5 class="fw-bold">${esc(e.title)}</h5><p class="text-muted small flex-grow-1">${esc(e.description)}</p><div class="border-top pt-3"><div class="small mb-1">📅 ${fmtDate(e.event_date)}</div><div class="small mb-1">🕒 ${fmtTime(e.event_time)}</div><div class="small mb-3">📍 ${esc(e.venue)}</div><div class="d-flex justify-content-between small"><b>Seats</b><span>${e.registered_count}/${e.capacity}</span></div><div class="progress progress-seat mb-3"><div class="progress-bar ${e.is_full?'bg-danger':'bg-success'}" style="width:${pct}%"></div></div><div class="d-grid">${action}</div></div></div></div>`}
function formDataObj(form){return Object.fromEntries(new FormData(form).entries())}
async function init(){const page=document.body.dataset.page; if(page==='home')return home();if(page==='login')return auth('login');if(page==='register')return auth('register');if(page==='student-dashboard')return studentDashboard();if(page==='events-browse')return browse();if(page==='event-details')return details();if(page==='schedule')return schedule();if(page==='announcements-view')return announcementsView();if(page==='admin-dashboard')return adminDashboard();if(page==='events-manage')return eventsManage();if(page==='categories-manage')return categoriesManage();if(page==='announcements-manage')return announcementsManage();if(page==='participants')return participants()}
async function home(){const app=layout('Home');await setupNav();app.innerHTML=`<section class="hero p-4 p-md-5 mb-4"><div class="row align-items-center g-4"><div class="col-lg-8"><span class="badge bg-light text-success mb-3">NSBM Green University</span><h1 class="display-5 fw-bold">Plan. Discover. Participate.</h1><p class="lead mb-4">A centralized event planning and scheduling platform for NSBM students, clubs and administrators.</p><a class="btn btn-light btn-lg me-2" href="events_browse.html">Browse Events</a><a class="btn btn-outline-light btn-lg" href="register.html">Create Account</a></div><div class="col-lg-4"><div class="hero-stat p-4"><h5>EventHub</h5><p class="mb-0">Manage campus events, registrations, announcements and personal schedules in one place.</p></div></div></div></section><div class="row g-4"><div class="col-md-4"><div class="glass-card p-4 h-100"><h5>For Students</h5><p class="text-muted">Find events, reserve seats and keep your schedule organized.</p></div></div><div class="col-md-4"><div class="glass-card p-4 h-100"><h5>For Administrators</h5><p class="text-muted">Create events, manage categories, publish notices and view participants.</p></div></div><div class="col-md-4"><div class="glass-card p-4 h-100"><h5>Connected Backend</h5><p class="text-muted">The frontend communicates with a dedicated PHP API and MySQL database.</p></div></div></div>`}
async function auth(kind){const app=layout(kind==='login'?'Login':'Register');await setupNav();app.innerHTML=`<div class="auth-card"><div class="glass-card p-4 p-md-5"><div class="text-center mb-4"><span class="navbar-brand-badge fs-3 d-inline-block mb-2">NSBM</span><h3 class="fw-bold">${kind==='login'?'Sign in to EventHub':'Create your EventHub account'}</h3></div><div id="formError"></div><form id="authForm"><div class="mb-3"><label class="form-label fw-semibold">${kind==='login'?'Email Address':'Full Name'}</label><input required name="${kind==='login'?'email':'full_name'}" class="form-control" type="${kind==='login'?'email':'text'}"></div>${kind==='register'?`<div class="mb-3"><label class="form-label fw-semibold">Email Address</label><input required name="email" class="form-control" type="email"></div><div class="mb-3"><label class="form-label fw-semibold">Register As</label><select name="role" class="form-select"><option value="student">Student</option><option value="admin">Administrator / Faculty</option></select></div>`:''}<div class="mb-3"><label class="form-label fw-semibold">Password</label><input required minlength="6" name="password" class="form-control" type="password"></div>${kind==='register'?`<div class="mb-4"><label class="form-label fw-semibold">Confirm Password</label><input required minlength="6" name="confirm_password" class="form-control" type="password"></div>`:'<div class="mb-4"></div>'}<button class="btn btn-nsbm w-100 py-2">${kind==='login'?'Log In':'Register Account'}</button></form><div class="text-center mt-3 small">${kind==='login'?`Don't have an account? <a href="register.html">Register</a>`:`Already have an account? <a href="login.html">Log in</a>`}</div></div></div>`;document.getElementById('authForm').onsubmit=async ev=>{ev.preventDefault();const body=formDataObj(ev.target);try{const r=await api(kind,{method:'POST',body});if(kind==='login')location.href=r.data.role==='admin'?'admin/dashboard.html':'student/dashboard.html';else location.href='login.html'}catch(e){document.getElementById('formError').innerHTML=`<div class="alert alert-danger">${esc(e.message)}</div>`}}}
async function studentDashboard(){const user=await requireRole('student');if(!user)return;const app=layout('Student Dashboard');await setupNav();const [ev,sch,ann]=await Promise.all([api('events'),api('schedule'),api('announcements')]);app.innerHTML=`<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="fw-bold mb-1">Welcome, ${esc(user.full_name)}</h2><p class="text-muted">Your NSBM EventHub overview.</p></div><a class="btn btn-nsbm" href="events_browse.html">Find Events</a></div><div class="row g-3 mb-4"><div class="col-md-4"><div class="glass-card p-4"><span class="text-muted">Upcoming Events</span><h2>${ev.data.length}</h2></div></div><div class="col-md-4"><div class="glass-card p-4"><span class="text-muted">My Registrations</span><h2>${sch.data.length}</h2></div></div><div class="col-md-4"><div class="glass-card p-4"><span class="text-muted">Announcements</span><h2>${ann.data.length}</h2></div></div></div><div class="glass-card p-4"><h5 class="fw-bold">Latest Events</h5><div class="row g-3 mt-1">${ev.data.slice(0,3).map(eventCard).join('')||'<p class="text-muted">No events available.</p>'}</div></div>`}
async function browse(){const user=await requireRole('student');if(!user)return;const app=layout('Browse Events');await setupNav();const [ev,cat]=await Promise.all([api('events'),api('categories')]);app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">Browse Campus Events</h2><p class="text-muted">Search by title, venue or description and filter by category.</p></div><div class="glass-card p-4 mb-4"><div class="row g-3"><div class="col-md-8"><input id="search" class="form-control" placeholder="Search events..."></div><div class="col-md-4"><select id="category" class="form-select"><option value="0">All Categories</option>${cat.data.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div></div></div><div id="grid" class="row g-4">${ev.data.map(eventCard).join('')||'<p class="text-muted">No events scheduled.</p>'}</div>`;const filter=()=>{const q=document.getElementById('search').value.toLowerCase(),c=document.getElementById('category').value;document.querySelectorAll('.event-item').forEach(x=>x.style.display=(x.dataset.search.includes(q)&&(c==='0'||x.dataset.category===c))?'':'none')};document.getElementById('search').oninput=filter;document.getElementById('category').onchange=filter}
async function details(){const user=await requireRole('student');if(!user)return;const id=new URLSearchParams(location.search).get('id');const app=layout('Event Details');await setupNav();const res=await fetch(`${API}?action=event&id=${encodeURIComponent(id)}`,{credentials:'same-origin'});const json=await res.json();if(!res.ok||!json.success){app.innerHTML='<div class="alert alert-danger">Event not found.</div>';return}const e=json.data;app.innerHTML=`<div class="row g-4"><div class="col-lg-8"><div class="glass-card p-4 p-md-5"><div class="d-flex justify-content-between mb-3"><span class="badge bg-success">${esc(e.category_name)}</span>${statusBadge(e.status)}</div><h2 class="fw-bold">${esc(e.title)}</h2><div class="row g-3 bg-light rounded-3 p-3 my-4"><div class="col-md-4"><small class="text-muted">Date</small><div class="fw-bold">${fmtDate(e.event_date)}</div></div><div class="col-md-4"><small class="text-muted">Time</small><div class="fw-bold">${fmtTime(e.event_time)}</div></div><div class="col-md-4"><small class="text-muted">Venue</small><div class="fw-bold">${esc(e.venue)}</div></div></div><h5>About This Event</h5><p class="text-secondary" style="white-space:pre-line">${esc(e.description)}</p><hr><h5>Event Updates</h5>${e.announcements.length?e.announcements.map(a=>`<div class="announcement p-3 bg-light rounded mb-2"><b>${esc(a.title)}</b><p class="small mb-0 mt-1">${esc(a.content)}</p></div>`).join(''):'<p class="text-muted">No updates posted.</p>'}</div></div><div class="col-lg-4"><div class="glass-card p-4 sticky-lg-top" style="top:90px"><h5>Seat Reservation</h5><p class="small text-muted">${e.registered_count} / ${e.capacity} seats taken</p><div class="progress mb-3"><div class="progress-bar ${e.is_full?'bg-danger':'bg-success'}" style="width:${Math.min(100,e.registered_count/e.capacity*100)}%"></div></div><p class="fw-semibold ${e.is_full?'text-danger':'text-success'}">${e.is_full?'No seats remaining':e.seats_left+' seats remaining'}</p><div id="action"></div><hr><small class="text-muted">Organizer</small><div class="fw-bold">${esc(e.organizer_name)}</div><div class="small text-muted">${esc(e.organizer_email)}</div></div></div></div>`;const action=document.getElementById('action');if(e.is_registered)action.innerHTML='<button id="cancel" class="btn btn-outline-danger w-100">Cancel My Registration</button>';else if(e.is_full||e.status==='Completed'||e.status==='Cancelled')action.innerHTML='<button class="btn btn-secondary w-100" disabled>Registration Closed</button>';else action.innerHTML='<button id="reserve" class="btn btn-nsbm w-100">Reserve My Seat</button>';const b=document.getElementById('reserve');if(b)b.onclick=async()=>{try{await api('registration',{method:'POST',body:{event_id:e.id,op:'register'}});toast('Registration successful!');setTimeout(()=>location.reload(),700)}catch(err){toast(err.message,'danger')}};const c=document.getElementById('cancel');if(c)c.onclick=async()=>{if(!confirm('Cancel this registration?'))return;try{await api('registration',{method:'POST',body:{event_id:e.id,op:'cancel'}});toast('Registration cancelled.');setTimeout(()=>location.reload(),700)}catch(err){toast(err.message,'danger')}}}
async function schedule(){const user=await requireRole('student');if(!user)return;const app=layout('My Schedule');await setupNav();const r=await api('schedule');app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">My Event Schedule</h2><p class="text-muted">Events you have registered for.</p></div><div class="glass-card p-4"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Event</th><th>Date</th><th>Time</th><th>Venue</th><th></th></tr></thead><tbody>${r.data.map(e=>`<tr><td><b>${esc(e.title)}</b><div class="small text-muted">${esc(e.category_name)}</div></td><td>${fmtDate(e.event_date)}</td><td>${fmtTime(e.event_time)}</td><td>${esc(e.venue)}</td><td><a class="btn btn-sm btn-outline-success" href="event_details.html?id=${e.id}">Details</a></td></tr>`).join('')||'<tr><td colspan="5" class="text-center text-muted py-4">No registrations yet.</td></tr>'}</tbody></table></div></div>`}
async function announcementsView(){const user=await requireRole('student');if(!user)return;const app=layout('Announcements');await setupNav();const r=await api('announcements');app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">Campus Announcements</h2><p class="text-muted">Latest notices from NSBM EventHub administrators.</p></div><div class="d-grid gap-3">${r.data.map(a=>`<div class="glass-card p-4"><div class="d-flex justify-content-between gap-3"><h5 class="fw-bold">${esc(a.title)}</h5><small class="text-muted">${new Date(a.created_at).toLocaleDateString()}</small></div><p class="mb-2">${esc(a.content)}</p><span class="badge bg-light text-dark border">${a.event_title?'Event: '+esc(a.event_title):'General Campus Notice'}</span></div>`).join('')||'<p class="text-muted">No announcements.</p>'}</div>`}
async function adminDashboard(){const user=await requireRole('admin');if(!user)return;const app=layout('Admin Dashboard');await setupNav();const r=await api('admin_dashboard');const c=r.data.counts;app.innerHTML=`<div class="d-flex justify-content-between mb-4"><div><h2 class="fw-bold">Admin Dashboard</h2><p class="text-muted">Manage the NSBM event ecosystem.</p></div><a class="btn btn-nsbm" href="events_manage.html">Create Event</a></div><div class="row g-3 mb-4">${[['Events',c.events],['Categories',c.categories],['Registrations',c.registrations],['Announcements',c.announcements]].map(x=>`<div class="col-md-3 col-6"><div class="glass-card p-3 stat-card"><span class="text-muted small">${x[0]}</span><h2>${x[1]}</h2></div></div>`).join('')}</div><div class="glass-card p-4"><h5>Recently Created Events</h5><div class="table-responsive"><table class="table"><thead><tr><th>Event</th><th>Category</th><th>Date</th><th>Status</th><th>Seats</th></tr></thead><tbody>${r.data.recent_events.map(e=>`<tr><td>${esc(e.title)}</td><td>${esc(e.category_name)}</td><td>${fmtDate(e.event_date)}</td><td>${statusBadge(e.status)}</td><td>${e.registered_count}/${e.capacity}</td></tr>`).join('')}</tbody></table></div></div>`}
async function eventsManage(){const user=await requireRole('admin');if(!user)return;const app=layout('Manage Events');await setupNav();const [ev,cat]=await Promise.all([api('events'),api('categories')]);app.innerHTML=`<div class="d-flex justify-content-between mb-4"><div><h2 class="fw-bold">Manage Events</h2><p class="text-muted">Create, edit and delete campus events.</p></div><button id="new" class="btn btn-nsbm">+ New Event</button></div><div id="editor"></div><div class="glass-card p-4"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Event</th><th>Category</th><th>Date</th><th>Status</th><th>Seats</th><th></th></tr></thead><tbody>${ev.data.map(e=>`<tr><td><b>${esc(e.title)}</b><div class="small text-muted">${esc(e.venue)}</div></td><td>${esc(e.category_name)}</td><td>${fmtDate(e.event_date)}</td><td>${statusBadge(e.status)}</td><td>${e.registered_count}/${e.capacity}</td><td><button class="btn btn-sm btn-outline-primary edit" data-id="${e.id}">Edit</button> <button class="btn btn-sm btn-outline-danger del" data-id="${e.id}">Delete</button></td></tr>`).join('')}</tbody></table></div></div>`;const editor=document.getElementById('editor');const show=(e=null)=>{editor.innerHTML=`<div class="glass-card p-4 mb-4"><h5>${e?'Edit Event':'Create Event'}</h5><form id="eventForm" class="row g-3"><input type="hidden" name="id" value="${e?.id||0}"><div class="col-md-8"><label class="form-label">Title</label><input required name="title" class="form-control" value="${esc(e?.title||'')}"></div><div class="col-md-4"><label class="form-label">Category</label><select required name="category_id" class="form-select">${cat.data.map(x=>`<option value="${x.id}" ${e?.category_id==x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></div><div class="col-12"><label class="form-label">Description</label><textarea required name="description" class="form-control" rows="4">${esc(e?.description||'')}</textarea></div><div class="col-md-3"><label class="form-label">Date</label><input required type="date" name="event_date" class="form-control" value="${e?.event_date||''}"></div><div class="col-md-3"><label class="form-label">Time</label><input required type="time" name="event_time" class="form-control" value="${e?.event_time||''}"></div><div class="col-md-3"><label class="form-label">Venue</label><input required name="venue" class="form-control" value="${esc(e?.venue||'')}"></div><div class="col-md-3"><label class="form-label">Capacity</label><input required type="number" min="1" name="capacity" class="form-control" value="${e?.capacity||50}"></div><div class="col-md-3"><label class="form-label">Status</label><select name="status" class="form-select">${['Upcoming','Ongoing','Completed','Cancelled'].map(s=>`<option ${e?.status===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="col-12"><button class="btn btn-nsbm">Save</button> <button type="button" id="closeEditor" class="btn btn-light border">Cancel</button></div></form></div>`;document.getElementById('closeEditor').onclick=()=>editor.innerHTML='';document.getElementById('eventForm').onsubmit=async x=>{x.preventDefault();try{const d=formDataObj(x);d.op=d.id>0?'update':'create';await api('events',{method:'POST',body:d});toast('Event saved.');setTimeout(()=>location.reload(),500)}catch(err){toast(err.message,'danger')}}};document.getElementById('new').onclick=()=>show();document.querySelectorAll('.edit').forEach(b=>b.onclick=()=>show(ev.data.find(x=>x.id==b.dataset.id)));document.querySelectorAll('.del').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this event?'))return;try{await api('events',{method:'POST',body:{op:'delete',id:b.dataset.id}});location.reload()}catch(err){toast(err.message,'danger')}})}
async function categoriesManage(){const user=await requireRole('admin');if(!user)return;const app=layout('Manage Categories');await setupNav();const r=await api('categories');app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">Manage Categories</h2><p class="text-muted">Create and maintain event classifications.</p></div><div class="row g-4"><div class="col-lg-5"><div class="glass-card p-4"><form id="catForm"><input type="hidden" name="id" value="0"><label class="form-label">Name</label><input required name="name" class="form-control mb-3"><label class="form-label">Description</label><textarea name="description" class="form-control mb-3" rows="4"></textarea><button class="btn btn-nsbm">Save Category</button></form></div></div><div class="col-lg-7"><div class="glass-card p-4"><div class="table-responsive"><table class="table"><thead><tr><th>Name</th><th>Events</th><th></th></tr></thead><tbody>${r.data.map(c=>`<tr><td><b>${esc(c.name)}</b><div class="small text-muted">${esc(c.description)}</div></td><td>${c.event_count}</td><td><button class="btn btn-sm btn-outline-danger del" data-id="${c.id}">Delete</button></td></tr>`).join('')}</tbody></table></div></div></div></div>`;document.getElementById('catForm').onsubmit=async e=>{e.preventDefault();try{await api('categories',{method:'POST',body:{...formDataObj(e.target),op:'create'}});location.reload()}catch(err){toast(err.message,'danger')}};document.querySelectorAll('.del').forEach(b=>b.onclick=async()=>{if(!confirm('Delete this category? Events using it will also be deleted because of the database foreign key.'))return;try{await api('categories',{method:'POST',body:{op:'delete',id:b.dataset.id}});location.reload()}catch(err){toast(err.message,'danger')}})}
async function announcementsManage(){const user=await requireRole('admin');if(!user)return;const app=layout('Manage Announcements');await setupNav();const [a,e]=await Promise.all([api('announcements'),api('events')]);app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">Campus Announcements</h2><p class="text-muted">Broadcast notices to students.</p></div><div class="row g-4"><div class="col-lg-5"><div class="glass-card p-4"><form id="annForm"><label class="form-label">Title</label><input required name="title" class="form-control mb-3"><label class="form-label">Event (optional)</label><select name="event_id" class="form-select mb-3"><option value="0">General Campus Notice</option>${e.data.map(x=>`<option value="${x.id}">${esc(x.title)}</option>`).join('')}</select><label class="form-label">Content</label><textarea required name="content" rows="5" class="form-control mb-3"></textarea><button class="btn btn-nsbm">Post Announcement</button></form></div></div><div class="col-lg-7"><div class="d-grid gap-3">${a.data.map(x=>`<div class="glass-card p-4"><div class="d-flex justify-content-between"><h5>${esc(x.title)}</h5><button class="btn btn-sm btn-outline-danger del" data-id="${x.id}">Delete</button></div><p>${esc(x.content)}</p><span class="badge bg-light text-dark border">${x.event_title?'Event: '+esc(x.event_title):'General Notice'}</span></div>`).join('')}</div></div></div>`;document.getElementById('annForm').onsubmit=async x=>{x.preventDefault();try{await api('announcements',{method:'POST',body:{...formDataObj(x.target),op:'create'}});location.reload()}catch(err){toast(err.message,'danger')}};document.querySelectorAll('.del').forEach(b=>b.onclick=async()=>{if(confirm('Delete announcement?')){await api('announcements',{method:'POST',body:{op:'delete',id:b.dataset.id}});location.reload()}})}
async function participants(){const user=await requireRole('admin');if(!user)return;const app=layout('Participants');await setupNav();const ev=await api('events');const id=new URLSearchParams(location.search).get('event_id')||ev.data[0]?.id;app.innerHTML=`<div class="mb-4"><h2 class="fw-bold">Participant Lists</h2><select id="eventSelect" class="form-select" style="max-width:500px">${ev.data.map(x=>`<option value="${x.id}" ${String(x.id)===String(id)?'selected':''}>${esc(x.title)}</option>`).join('')}</select></div><div id="list"></div>`;const load=async()=>{const res=await fetch(`${API}?action=participants&event_id=${document.getElementById('eventSelect').value}`,{credentials:'same-origin'});const j=await res.json();document.getElementById('list').innerHTML=`<div class="glass-card p-4"><div class="table-responsive"><table class="table"><thead><tr><th>#</th><th>Student</th><th>Email</th><th>Registered</th></tr></thead><tbody>${(j.data||[]).map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.full_name)}</td><td>${esc(x.email)}</td><td>${new Date(x.registered_at).toLocaleString()}</td></tr>`).join('')||'<tr><td colspan="4" class="text-center text-muted">No participants.</td></tr>'}</tbody></table></div></div>`};document.getElementById('eventSelect').onchange=load;load()}
document.addEventListener('DOMContentLoaded',init);
